# 2026-06-20 会话记录

## 决策 DECISION（续 23:00+）
- 采纳用户建议：Skills 最关键问题是 Trae 不自动调用 → 所有 18 个 skill 新增 keywords/tags/trigger 中英文触发词
- 优化策略：按分数从低到高排序，每轮改 1 个维度 + 触发词增强
- 仅安装轻量外部技能（caveman 7 个 + superpowers 14 个），跳过超重包（everything-claude-code 3251 文件、claude-mem 934 文件）
- 创建 agent-router 外部 Agent 路由分发技能
- 覆盖冲突处理：superpowers 的 systematic-debugging 覆盖了优化版，已重建合并（原版工作流 + 优化触发词）

## 数据 DATA（新增 23:00+）
- 基线评估：18 skills 平均 72.7 → 优化后平均 78.7（+6.0）
- 最差 5 个已全部脱离危险区（brand-guidelines 38→66、find-skills 50→65 等）
- 15 次优化 commit，0 回滚
- caveman 已通过 `npx caveman` 安装到 skills 系统
- superpowers 14 个 skill 已复制到 `/data/user/skills/`
- agent-router skill 已创建（路由矩阵 + 4 阶段工作流 + 交接模板）
- 新创建文件：`/workspace/agent-router/SKILL.md`、`/workspace/superpowers/` 克隆、`/workspace/caveman/` 克隆

## 文件 FILE（更新）
- `/workspace/auto-optimize-results.tsv`（18 skills 基线+18 条优化记录）
- `/workspace/test-prompts/*.json`（18 个测试 prompt 文件）
- `/workspace/agent-router/SKILL.md`（新增外部 Agent 路由 skill）
- `/workspace/superpowers/`（克隆的 14 个 skill）
- `/workspace/caveman/`（克隆的 7 个 skill，`npx caveman` 已安装）
- `/workspace/memory/MEMORY.md`（更新优化完成状态）

## 下一步 NEXT
- OrdPaw 主线任务等待仓库 URL
- 如有需求可对新增的 caveman/superpowers skill 运行基线评估 + 优化
- 启用 memory-booster 作为长期跨会话记忆系统。
- 记忆目录固定在 `/workspace/.workbuddy/memory`，语义索引放在 `/workspace/memory-booster/chroma_db`。
- 运行 memory-booster 脚本必须使用 Python 3.13.13（`PYENV_VERSION=3.13.13`），因为 Python 3.14 与 tokenizers/PyO3 不兼容。
- 主线任务切换为 **OrdPaw**：增加批量/多选下载能力（AI 对话、AI 生成程序、文件、Skills、MCP、Script、OrdPaw 源码等），支持存储位置与空间限制设置，支持 Browser/Server 分散存储，优化 New UI/UX 动画性能与视觉品质。
- Skills 全量优化任务暂停，状态已保存。

## 数据 DATA
- 已安装依赖：chromadb==0.5.23、sentence-transformers==3.3.1（约 532MB torch + 相关包）。
- 语义索引已建立：MEMORY.md 6 chunks。
- 已修复 `memory-booster/scripts/index_memory.py` 第 106 行 bug：`get_memory_files()` 改为 `get_memory_files(MEMORY_DIRS)`。
- 已记录 hot memory 条目：外部 Agent 路由规则、Skills 优化状态、OrdPaw 主线任务。

## 文件 FILE
- `/workspace/.workbuddy/memory/MEMORY.md`
- `/workspace/.workbuddy/memory/2026-06-20.md`
- `/workspace/memory-booster/chroma_db/`
- `/data/user/skills/memory-booster/config.json`（已更新 memory_dirs/chroma_db_dir）
- `/data/user/skills/memory-booster/scripts/index_memory.py`（已 patch）
- `/workspace/test-prompts/*.json`（18 个 skill 的测试 prompt）
- `/workspace/auto-optimize-results.tsv`

## 关联 LINK
- 外部 Agent 调用规则：需要 Qwen/WorkBuddy/OpenCode/Z.ai 时，整合全部上下文到单一文件，再由用户自动路由。
- Skills 优化分支：`/data/user/skills`，`auto-optimize/20260620-0015`。

## 下一步 NEXT
- 等待用户提供 OrdPaw Git 仓库 URL，clone 到 `/workspace/ordpaw`。
- 分析 OrdPaw 代码结构，设计下载管理器（批量/多选、存储位置、空间限制、分散存储 Browser/Server）。
- 设计并实现 New UI/UX 性能优化与视觉品质提升。

## 问题 ISSUE
- memory-booster 原脚本在 Python 3.14 下无法安装 tokenizers；已降级到 Python 3.13.13 解决。
- memory-booster `index_memory.py` 原代码存在调用参数缺失 bug，已本地 patch。
